<%if(error && error.length>0){%>
    <div class="text-center pb-2 text-red-400"><%=error[0]%></div>
<%}%>

<%if(success && success.length>0){%>
    <div class="text-center py-2 text-green-500"><%=success[0]%></div>
<%}%>